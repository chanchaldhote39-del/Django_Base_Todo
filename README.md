# Django_Base_Todo
A Django-based To-Do Application is a small web project that helps users create, update, and manage their daily tasks. It uses Django’s MVC/MVT structure and provides a simple interface through the Django Admin or custom frontend.
